enum ClauseKeys {
    DISTINCT = 'distinct',
    SELECT = 'select',
    FROM = 'from',
    JOIN = 'join',
    WHERE = 'where',
    GROUP_BY = 'groupBy',
    HAVING = 'having',
    ORDER_BY = 'orderBy',
    UNION = 'union',
    UNION_ORDER = 'unionOrder',
    LIMIT = 'limit',
    OFFSET = 'offset',
}

export default ClauseKeys;
